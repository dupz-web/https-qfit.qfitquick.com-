// Vite 루트는 app/ 이고 실제 코드는 ../src/ 에 있다.
//
// HTML 에서 "../src/main.js" 로 바로 가리키면 dev 서버가 루트 밖이라 못 찾는다
// (브라우저가 /src/main.js 로 풀어 버린다). 자바스크립트 import 는 Vite 의
// 모듈 해석기를 타서 루트 밖도 정상으로 잡히므로, 한 줄만 여기에 둔다.
//
// src/ 를 app/ 안으로 옮기면 이 파일이 필요 없지만, 그러면 검사 스크립트
// 열두 개의 경로가 따라 바뀐다. 그 값에 비해 얻는 게 없다.
import '../src/main.js';
